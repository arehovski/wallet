version: '3.9'

services:
  db:
    image: postgres:latest
    ports:
      - "5432"
    env_file:
      .env.test

  tests:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    env_file:
      .env.test
